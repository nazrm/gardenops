# Contributing

Use the setup instructions in `README.md`, run the relevant tests before opening
a pull request, and avoid committing local environment files, generated build
output, screenshots, backups, logs, database dumps, or deployment secrets.
